'use client'

import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { getStreetTiles } from '@/lib/game/streetTiles'
import { BUILDINGS } from '@/lib/game/buildings/index'
import { BuildingSVG, BuildingSpriteStyles } from '@/lib/grid/BuildingSVG'

interface TileEntity { id:string; entity_id:string; entity_type:string; tile_row:number; tile_col:number; profile_id:string|null; owner_class:string; actor_name?:string|null; username?:string|null }
interface Ship { id:string; ship_type:string; is_active:boolean; location_id:string }
interface Assignment { type:string; roleCode:string|null; tileEntityId:string|null }
interface Need { code:string; satisfaction:number }
interface Skill { code:string; level:number; experience:number }
interface Resident { id:string; displayName:string; birthYear:number|null; activityState:string; lastAction:string|null; assignments:Assignment[]; needs:Need[]; skills:Skill[] }
interface Props { locationSlug:string; locationName:string; population:number; entities:TileEntity[]; pending:any[]; ships:Ship[]; locationId:string; userId:string; onClose:()=>void; onEnterBuilding?:(entity:TileEntity)=>void }
type Selection = { kind:'building'; id:string } | { kind:'person'; id:string } | null

const COLS=32, ROWS=24, ISO_W=32, ISO_H=16
const CANVAS_W=(COLS+ROWS)*ISO_W+100, CANVAS_H=(COLS+ROWS)*ISO_H+200, MIN_ZOOM=.65, MAX_ZOOM=1.65
const C={road:'#5a5040',roadMain:'#6a6050',crossing:'#7a7060',figure:'#c9a961',npc:'#8fbca6',ship:'#6a9aca',text:'#e4dccd',selected:'#f1d57a'}
function isoProject(col:number,row:number){return{x:(col-row)*ISO_W+CANVAS_W/2,y:(col+row)*ISO_H+60}}
function labelForBuilding(id:string){return BUILDINGS[id]?.name ?? id.replace(/_/g,' ').replace(/\b\w/g,m=>m.toUpperCase())}
function pct(v:number){return `${Math.round(v*100)}%`}
function assignmentOf(r:Resident,type:string){return r.assignments.find(a=>a.type===type)}
function drawFigure(ctx:CanvasRenderingContext2D,x:number,y:number,color:string,label=''){ctx.fillStyle='rgba(0,0,0,.32)';ctx.beginPath();ctx.ellipse(x,y+2,6,2.5,0,0,Math.PI*2);ctx.fill();const b=y-3;ctx.fillStyle='#1b2936';ctx.fillRect(x-2.5,b-6,2,6);ctx.fillRect(x+.5,b-6,2,6);ctx.fillStyle='#2c5574';ctx.fillRect(x-3.5,b-14,7,8);ctx.fillStyle=color;ctx.beginPath();ctx.arc(x,b-17,4,0,Math.PI*2);ctx.fill();if(label){ctx.fillStyle=C.text;ctx.font='bold 7px monospace';ctx.textAlign='center';ctx.fillText(label,x,b-24)}}
function drawIsoFloor(ctx:CanvasRenderingContext2D,col:number,row:number,color:string){const{x,y}=isoProject(col,row);ctx.fillStyle=color;ctx.beginPath();ctx.moveTo(x,y-ISO_H);ctx.lineTo(x+ISO_W,y);ctx.lineTo(x,y+ISO_H);ctx.lineTo(x-ISO_W,y);ctx.closePath();ctx.fill()}
function drawShip(ctx:CanvasRenderingContext2D,cx:number,cy:number){ctx.fillStyle='rgba(0,0,0,.3)';ctx.beginPath();ctx.ellipse(cx,cy+20,20,6,0,0,Math.PI*2);ctx.fill();ctx.fillStyle=C.ship;ctx.beginPath();ctx.moveTo(cx,cy-22);ctx.lineTo(cx-16,cy+6);ctx.lineTo(cx-11,cy+18);ctx.lineTo(cx+11,cy+18);ctx.lineTo(cx+16,cy+6);ctx.closePath();ctx.fill()}
const panelStyle:React.CSSProperties={background:'#111926',border:'2px solid #5f6d7a',boxShadow:'inset 0 0 0 1px #0b1018',color:'#d8d4c8'}
const sectionStyle:React.CSSProperties={borderTop:'1px solid #465363',paddingTop:8,marginTop:8}
const actionStyle:React.CSSProperties={width:'100%',padding:'8px 10px',background:'#27394d',color:'#f2df9c',border:'2px outset #58697c',font:'bold 11px monospace',cursor:'pointer'}

export default function WalkableColony({locationSlug,locationName,population,entities,pending,ships,userId,onClose,onEnterBuilding}:Props){
 const canvasRef=useRef<HTMLCanvasElement>(null),dragRef=useRef<{x:number;y:number;vx:number;vy:number}|null>(null)
 const[figPos,setFigPos]=useState({col:4.5,row:8.5}),[viewport,setViewport]=useState({x:Math.max(0,CANVAS_W/2-400),y:0}),[zoom,setZoom]=useState(1),[dragging,setDragging]=useState(false),[residents,setResidents]=useState<Resident[]>([]),[selection,setSelection]=useState<Selection>(null),[showPeople,setShowPeople]=useState(false)
 const buildings=useMemo(()=>entities.filter(e=>e.entity_type==='building'),[entities])
 const streets=useMemo(()=>getStreetTiles(locationSlug,population,entities,pending,userId,COLS,ROWS),[locationSlug,population,entities,pending,userId])
 const playerHabitat=entities.find(e=>e.entity_id==='habitat'&&e.profile_id===userId),landingPad=entities.find(e=>e.entity_id==='landing_pad'||e.entity_id==='docking_bay'),hasShipAtLocation=ships.some(s=>s.is_active)
 const selectedBuilding=selection?.kind==='building'?entities.find(e=>e.id===selection.id)??null:null,selectedPerson=selection?.kind==='person'?residents.find(r=>r.id===selection.id)??null:null
 const clampViewport=useCallback((x:number,y:number,z=zoom)=>({x:Math.max(0,Math.min(CANVAS_W-800/z,x)),y:Math.max(0,Math.min(CANVAS_H-500/z,y))}),[zoom])
 const centerOn=useCallback((col:number,row:number,z=zoom)=>{const p=isoProject(col,row);setViewport(clampViewport(p.x-400/z,p.y-250/z,z))},[zoom,clampViewport])
 useEffect(()=>{if(playerHabitat){const p={col:playerHabitat.tile_col+.5,row:playerHabitat.tile_row+.5};setFigPos(p);centerOn(p.col,p.row)}},[playerHabitat?.id])
 useEffect(()=>{let live=true;fetch(`/api/game/population?locationSlug=${encodeURIComponent(locationSlug)}`).then(r=>r.ok?r.json():Promise.reject()).then(d=>{if(live)setResidents(Array.isArray(d.residents)?d.residents:[])}).catch(()=>{if(live)setResidents([])});return()=>{live=false}},[locationSlug])
 const residentPositions=useMemo(()=>residents.map((r,i)=>{const a=assignmentOf(r,'work')??r.assignments.find(x=>x.tileEntityId),b=entities.find(e=>e.id===a?.tileEntityId);if(!b)return null;const slot=i%4;return{resident:r,col:b.tile_col+.18+(slot%2)*.38,row:b.tile_row+.15+Math.floor(slot/2)*.34,building:b}}).filter(Boolean) as {resident:Resident;col:number;row:number;building:TileEntity}[],[residents,entities])
 useEffect(()=>{const canvas=canvasRef.current;if(!canvas)return;const ctx=canvas.getContext('2d');if(!ctx)return;ctx.clearRect(0,0,CANVAS_W,CANVAS_H);ctx.fillStyle='#090e17';ctx.fillRect(0,0,CANVAS_W,CANVAS_H);for(let r=0;r<ROWS;r++)for(let c=0;c<COLS;c++){const v=((c*7+r*13)%6)*4,s=streets.find(x=>x.col===c&&x.row===r),color=s?(s.subtype==='main'?C.roadMain:s.subtype==='crossing'?C.crossing:C.road):`rgb(${56+v},${49+v},${39+v})`;drawIsoFloor(ctx,c,r,color)};if(landingPad&&hasShipAtLocation){const p=isoProject(landingPad.tile_col,landingPad.tile_row);drawShip(ctx,p.x,p.y-35)};for(const rp of residentPositions){const p=isoProject(rp.col,rp.row);drawFigure(ctx,p.x,p.y,selection?.kind==='person'&&selection.id===rp.resident.id?C.selected:C.npc)}const p=isoProject(figPos.col,figPos.row);drawFigure(ctx,p.x,p.y,C.figure,'Du')},[figPos,streets,landingPad,hasShipAtLocation,residentPositions,selection])
 const selectBuilding=(e:TileEntity)=>{setSelection({kind:'building',id:e.id});setShowPeople(false)}
 const focusPerson=(r:Resident)=>{setSelection({kind:'person',id:r.id});setShowPeople(false);const rp=residentPositions.find(x=>x.resident.id===r.id);if(rp)centerOn(rp.col,rp.row)}
 const enterSelected=()=>{if(selectedBuilding&&onEnterBuilding)onEnterBuilding(selectedBuilding)}
 const wheel=useCallback((e:React.WheelEvent)=>{e.preventDefault();const next=Math.max(MIN_ZOOM,Math.min(MAX_ZOOM,zoom*(e.deltaY>0?.9:1.1)));setZoom(next);setViewport(v=>clampViewport(v.x,v.y,next))},[zoom,clampViewport])
 const pointerDown=(e:React.PointerEvent)=>{dragRef.current={x:e.clientX,y:e.clientY,vx:viewport.x,vy:viewport.y};setDragging(false);e.currentTarget.setPointerCapture(e.pointerId)},pointerMove=(e:React.PointerEvent)=>{const d=dragRef.current;if(!d)return;const dx=(e.clientX-d.x)/zoom,dy=(e.clientY-d.y)/zoom;if(Math.abs(dx)+Math.abs(dy)>4)setDragging(true);setViewport(clampViewport(d.vx-dx,d.vy-dy))},pointerUp=(e:React.PointerEvent)=>{dragRef.current=null;if(e.currentTarget.hasPointerCapture(e.pointerId))e.currentTarget.releasePointerCapture(e.pointerId);setTimeout(()=>setDragging(false),0)}
 useEffect(()=>{const fn=(e:KeyboardEvent)=>{if(e.key==='Escape'){if(selection){setSelection(null);return}onClose();return}if(e.key==='Enter'&&selectedBuilding){enterSelected();return}if(e.key.toLowerCase()==='f'){centerOn(figPos.col,figPos.row);return}const step=36/zoom;if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight','w','a','s','d'].includes(e.key)){e.preventDefault();setViewport(v=>clampViewport(v.x+(e.key==='ArrowRight'||e.key==='d'?step:e.key==='ArrowLeft'||e.key==='a'?-step:0),v.y+(e.key==='ArrowDown'||e.key==='s'?step:e.key==='ArrowUp'||e.key==='w'?-step:0)))}};window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[onClose,selection,selectedBuilding,zoom,clampViewport,centerOn,figPos])
 const workers=selectedBuilding?residents.filter(r=>r.assignments.some(a=>a.type==='work'&&a.tileEntityId===selectedBuilding.id)):[],homeResidents=selectedBuilding?residents.filter(r=>r.assignments.some(a=>a.type==='home'&&a.tileEntityId===selectedBuilding.id)):[]
 const lowNeed=selectedPerson?[...selectedPerson.needs].sort((a,b)=>a.satisfaction-b.satisfaction)[0]:null,work=selectedPerson?assignmentOf(selectedPerson,'work'):null,home=selectedPerson?assignmentOf(selectedPerson,'home'):null
 const workBuilding=work?.tileEntityId?entities.find(e=>e.id===work.tileEntityId):null,homeBuilding=home?.tileEntityId?entities.find(e=>e.id===home.tileEntityId):null
 const worldTransform=`translate(${-viewport.x*zoom}px,${-viewport.y*zoom}px) scale(${zoom})`
 return <div style={{position:'absolute',inset:0,zIndex:100,background:'#0b1018',display:'grid',gridTemplateRows:'38px minmax(0,1fr) 46px',border:'3px ridge #6a7480',fontFamily:'monospace'}}><BuildingSpriteStyles/>
  <header style={{display:'flex',justifyContent:'space-between',alignItems:'center',padding:'0 10px',background:'#1a2736',borderBottom:'2px ridge #6a7480',color:'#f1d57a'}}><div style={{fontWeight:700,fontSize:12}}>NOXIA · {locationName.toUpperCase()} <span style={{color:'#9eabb8',fontWeight:400}}>POP {population.toLocaleString()} · SIM {residents.length}</span></div><div style={{display:'flex',gap:6}}><button onClick={()=>setShowPeople(v=>!v)} style={{...actionStyle,width:'auto',padding:'4px 9px'}}>PERSONEN [{residents.length}]</button><button onClick={onClose} style={{...actionStyle,width:'auto',padding:'4px 9px'}}>KARTE SCHLIESSEN</button></div></header>
  <main style={{minHeight:0,display:'grid',gridTemplateColumns:'minmax(0,1fr) 260px'}}><div style={{position:'relative',overflow:'hidden',background:'#050912'}} onWheel={wheel} onPointerDown={pointerDown} onPointerMove={pointerMove} onPointerUp={pointerUp}>
   <canvas ref={canvasRef} width={CANVAS_W} height={CANVAS_H} style={{position:'absolute',left:0,top:0,transformOrigin:'0 0',transform:worldTransform,pointerEvents:'none'}}/>
   <div style={{position:'absolute',left:0,top:0,width:CANVAS_W,height:CANVAS_H,transformOrigin:'0 0',transform:worldTransform,pointerEvents:'none'}}>{buildings.map(b=>{const p=isoProject(b.tile_col,b.tile_row),selected=selection?.kind==='building'&&selection.id===b.id;return <button key={b.id} title={`${labelForBuilding(b.entity_id)} — Klick auswählen, Doppelklick betreten`} onPointerDown={e=>e.stopPropagation()} onClick={e=>{e.stopPropagation();selectBuilding(b)}} onDoubleClick={e=>{e.stopPropagation();onEnterBuilding?.(b)}} style={{position:'absolute',left:p.x-26,top:p.y-54,width:52,height:58,padding:0,background:selected?'rgba(241,213,122,.16)':'transparent',border:selected?'2px solid #f1d57a':'1px solid transparent',borderRadius:5,cursor:'pointer',pointerEvents:'auto'}}><BuildingSVG entityId={b.entity_id} planet={locationSlug} owned={b.profile_id===userId} size={48}/><span style={{position:'absolute',top:47,left:'50%',transform:'translateX(-50%)',whiteSpace:'nowrap',font:'7px monospace',color:'#e4dccd',textShadow:'0 1px 2px #000'}}>{labelForBuilding(b.entity_id)}</span></button>})}</div>
   <div style={{position:'absolute',left:8,top:8,padding:'5px 7px',background:'rgba(9,14,23,.84)',border:'1px solid #536273',color:'#aebbc7',fontSize:9}}>KLICK: GEBÄUDE AUSWÄHLEN · DOPPELKLICK/ENTER: BETRETEN · ZIEHEN/WASD: KAMERA · F: FIGUR</div><div style={{position:'absolute',right:8,bottom:8,display:'flex',gap:3}}><button onClick={()=>setZoom(z=>Math.max(MIN_ZOOM,z-.15))}>−</button><button onClick={()=>centerOn(figPos.col,figPos.row)}>F</button><button onClick={()=>setZoom(z=>Math.min(MAX_ZOOM,z+.15))}>+</button></div>
  </div><aside style={{...panelStyle,minHeight:0,overflowY:'auto',padding:10}}>{showPeople?<><div style={{fontSize:11,fontWeight:700,color:'#f1d57a',marginBottom:8}}>BEVÖLKERUNG · {residents.length}</div>{residents.map(r=>{const w=assignmentOf(r,'work');return <button key={r.id} onClick={()=>focusPerson(r)} style={{display:'block',width:'100%',textAlign:'left',padding:7,marginBottom:5,background:'#172231',border:'1px solid #536273',color:'#ddd6c7',cursor:'pointer'}}><strong>{r.displayName}</strong><br/><span style={{fontSize:9,color:'#8fbca6'}}>{w?.roleCode??r.activityState} · {r.lastAction??'idle'}</span></button>})}</>:selectedBuilding?<><div style={{fontSize:10,color:'#8fa1b4'}}>GEBÄUDE</div><div style={{fontSize:15,fontWeight:700,color:'#f1d57a',margin:'2px 0 8px'}}>{labelForBuilding(selectedBuilding.entity_id)}</div><div style={{fontSize:10,lineHeight:1.55}}>Koordinate {selectedBuilding.tile_col},{selectedBuilding.tile_row}<br/>Eigentum: {selectedBuilding.profile_id===userId?'du':selectedBuilding.owner_class==='STATE'?'staatlich':selectedBuilding.actor_name??selectedBuilding.username??'fremd'}</div><div style={sectionStyle}>PERSONAL · {workers.length} Arbeitende · {homeResidents.length} Wohnende{workers.slice(0,5).map(r=><button key={r.id} onClick={()=>focusPerson(r)} style={{display:'block',background:'none',border:0,color:'#8fbca6',padding:'3px 0',cursor:'pointer',font:'10px monospace'}}>› {r.displayName} · {assignmentOf(r,'work')?.roleCode}</button>)}</div><div style={sectionStyle}><button onClick={enterSelected} style={actionStyle}>BETRETEN</button></div></>:selectedPerson?<><div style={{fontSize:10,color:'#8fa1b4'}}>PERSON</div><div style={{fontSize:15,fontWeight:700,color:'#f1d57a'}}>{selectedPerson.displayName}</div><div style={{fontSize:10,color:'#8fbca6'}}>{work?.roleCode??selectedPerson.activityState} · {selectedPerson.lastAction??'idle'}</div><div style={sectionStyle}>Arbeit: {workBuilding?labelForBuilding(workBuilding.entity_id):'—'}<br/>Wohnen: {homeBuilding?labelForBuilding(homeBuilding.entity_id):'—'}</div><div style={sectionStyle}>BEDÜRFNISSE{selectedPerson.needs.map(n=><div key={n.code} style={{fontSize:9}}>{n.code}: {pct(n.satisfaction)}</div>)}</div><div style={sectionStyle}>KOMPETENZEN{selectedPerson.skills.map(s=><div key={s.code} style={{fontSize:9}}>{s.code}: {pct(s.level)} · XP {s.experience}</div>)}</div><div style={sectionStyle}>BEOBACHTUNG<br/><span style={{fontSize:9}}>{lowNeed&&lowNeed.satisfaction<.6?`${lowNeed.code} ist auffällig niedrig (${pct(lowNeed.satisfaction)}).`:'Keine kritische persönliche Abweichung erkannt.'}</span></div></>:<><div style={{fontSize:11,fontWeight:700,color:'#f1d57a'}}>KOLONIEKONTROLLE</div><div style={{fontSize:10,lineHeight:1.6,marginTop:8}}>Gebäude sind wieder als echte NOXIA-Sprites sichtbar. Klick wählt aus; Doppelklick oder BETRETEN öffnet den Innenraum.</div></>}</aside></main>
  <footer style={{display:'grid',gridTemplateColumns:'1fr auto',alignItems:'center',padding:'0 10px',background:'#172230',borderTop:'2px ridge #6a7480',color:'#9eabb8',fontSize:9}}><div>{selection?.kind==='building'?`AUSWAHL: ${selectedBuilding?labelForBuilding(selectedBuilding.entity_id):'—'}`:selection?.kind==='person'?`AUSWAHL: ${selectedPerson?.displayName??'—'}`:'BEREIT'} · {buildings.length} GEBÄUDE · {residents.length} SIMULIERTE PERSONEN</div><div>ZOOM {Math.round(zoom*100)}% · KAMERA {Math.round(viewport.x)},{Math.round(viewport.y)}</div></footer>
 </div>
}
