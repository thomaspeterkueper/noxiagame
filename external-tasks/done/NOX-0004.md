# NOX-0004 — vercel.json: fehlende Cron-Jobs wiederherstellen

Status: done
Completed: 2026-07-08

## Resolution

Bei Überprüfung war vercel.json bereits vollständig:

```json
{
  "crons": [
    { "path": "/api/cron/population", "schedule": "0 8 * * *" },
    { "path": "/api/cron/prices", "schedule": "*/30 * * * *" },
    { "path": "/api/cron/orders", "schedule": "15 * * * *" }
  ]
}
```

Alle drei Crons (population, prices, orders) vorhanden.
SETUP.md ebenfalls korrekt dokumentiert.
Task war bereits erledigt.
