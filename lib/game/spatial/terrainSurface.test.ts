import { sampleTerrainSurface, withTerrainHillshade } from './terrainSurface'
import type { TerrainSampler } from './terrainSampling'
import type { TerrainDatasetDescriptor, WorldFrame } from './types'
function assert(condition:boolean,message:string){if(!condition)throw new Error(message)}
const frame:WorldFrame={locationId:'surface-fixture',body:'moon',coordinateSystem:'LOCAL_ENU_METERS',originLatDeg:-89.67,originLonDeg:129.78,originAltM:0,originStatus:'verified',referenceFrame:'IAU_MOON_MEAN_SPHERE',latitudeType:'planetocentric',longitudeDirection:'positive_east',equatorialRadiusM:1737400,polarRadiusM:1737400,verticalDatum:'MEAN_RADIUS_1737400_M',terrainDatasetId:'surface-fixture',worldSeed:'SURFACE'}
const dataset:TerrainDatasetDescriptor={id:'surface-fixture',body:'moon',provider:'fixture',datasetName:'surface fixture',datasetKind:'dem',horizontalReference:'MOON_MEAN_EARTH_POLAR_AXIS',verticalReference:'MEAN_RADIUS_1737400_M',latitudeType:'planetocentric',longitudeDirection:'positive_east',sourceUri:'fixture://surface',accessMode:'fixture',status:'ready'}
const sampler:TerrainSampler={async sampleTerrainHeight(_context,point){if(point.xM===20&&point.yM===20)return null;return{xM:point.xM,yM:point.yM,zM:point.xM*.1+point.yM*.2,sourceElevationM:1000,datasetId:dataset.id,verticalReference:dataset.verticalReference}}}
const surface=await sampleTerrainSurface(sampler,{frame,dataset},{minXM:0,minYM:0,maxXM:20,maxYM:20,columns:3,rows:3});assert(surface.resolvedCount===8,'NoData must remain unresolved');assert(surface.cells[8].zM===null,'missing sample must remain null');const shaded=withTerrainHillshade(surface);assert(shaded.cells[4].hillshade!==null,'complete neighbourhood must produce hillshade');assert(shaded.cells[8].hillshade===null,'NoData must remain unshaded');console.log('terrain surface projection tests passed')
